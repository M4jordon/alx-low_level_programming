#include <stdio.h>
#include "main.h"

void positive_or_negative(int i)

{
        if (i < 0)
                printf("%d is negative number\n", i);
        else
                printf("%d is postivie number\n", i);
}
